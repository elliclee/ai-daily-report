# AI早报每日生成（融合版·8点）- 当前 cron prompt
# jobId: 50702738-9cbe-4279-9ba3-871991087f5c

执行 AI 日报生成任务（强时效版，完全按 2026-02-13 规范执行；A/B/C 对齐）。

0) 总体策略：分层执行（控制成本，避免 20+ query 全跑）
- 第一层：Breaking 12（固定必跑）
  - 英文 6 + 中文 6；全部 freshness=pd（过去24h）
- 第二层：按需补搜（动态触发，4~8 条上限）
  - 触发：栏目缺口 / 漏报检查命中但缺权威来源 / 发布时间不可确认
  - 默认 freshness=pw（7d），并在条目 meta 标注「7d补漏」
- 第三层：二次补时间（必须做一次）
  - 若发布时间无法确认 ≤48h：必须再找第二来源补时间；补不到 → 降级到「观察/补充」，不得进“头条/核心看点”

1) Web（web_search）Breaking 固定 12（pd）
英文 6：
1. AI model release
2. OpenAI GPT API pricing
3. Anthropic Claude release update
4. Google Gemini release update
5. Meta Llama open source weights
6. AI security incident outage vulnerability regulation

中文 6（按 3+3）：
7. AI 大模型 发布 上线 更新 24小时
8. AI 开源 权重 GitHub 24小时
9. AI 漏洞 故障 监管 合规 24小时
10. 字节 Seedance 即梦 豆包 发布 上线 24小时
11. 智谱 GLM 发布 更新 开源 24小时
12. 月之暗面 Kimi 通义 Qwen 混元 文心 更新 发布 24小时

硬规则（时效过滤）：
- 任何条目若无法确认发布时间 ≤48h：
  - 禁止进入“核心看点”
  - 必须做 1 次第二来源补时间
  - 补不到：归入「观察/补充」，标注 [时间无法确认，未入头条]

2) X（bird）策略：关键词组 + watchlist 双轨 + 24h 过滤（带 fallback）
2.1 关键词组（每组取最新 N 条，建议 N=30）
英文 4 组：
- OpenAI OR GPT OR ChatGPT OR API
- Anthropic OR Claude OR "Claude Code"
- Gemini OR DeepMind OR "Google AI"
- Llama OR "Meta AI" OR open-source weights
中文 4 组（避免混杂，按你建议拆分）：
- Seedance OR 即梦 OR 豆包 OR AI视频
- 智谱 OR GLM
- 月之暗面 OR Kimi
- 通义 OR Qwen
- 混元 OR 文心 OR 百度AI OR 腾讯AI
（若要保持总组数不变：可将英文 Llama 组与 MetaAI/watchlist 合并，确保总查询组数不膨胀）

2.2 watchlist（近24h）
- @OpenAI @sama @AnthropicAI @GoogleDeepMind @GeminiApp @MetaAI（+你的增补清单）

2.3 近24h过滤 & fallback（bird 时间戳不可靠时必须用）
- 优先：若支持 since/until 等 time-bounded query → 用它
- 否则 fallback（必须执行）：
  1) 只取“最新 N 条”（如 30）
  2) 有效性筛选（满足其一即可入候选）：来自 watchlist / 互动阈值达标 / 被多账号重复提及
  3) 再做时间判定：相对时间>48h剔除；时间缺失→降级「观察/补充」
互动阈值建议：likes/retweets/replies 任一 ≥20（可调）；来自 watchlist 直接放行。

3) 栏目规则（避免硬塞水货）
- Releases / Updates / Open Source / Benchmarks / Business：目标 2，下限 1；若 <1 → 触发补搜
- Risks：目标 2，下限 0；若为 0 必须写：过去24h未发现可靠重大风险事件（已按X+Web补搜）

4) 去重合并：可执行 dedupe_key（必须生成并在自检区输出）
- dedupe_key = (entity, product, event_type, version_or_date)
  - entity：OpenAI/Anthropic/字节/智谱/…
  - product：GPT/Claude/Seedance/GLM/…
  - event_type：release/update/open_source/benchmark/business/risk
  - version_or_date：vX.Y / YYYY-MM-DD / pricing-change 等
- 同 dedupe_key 必须合并，sources 累积到 2~3 个
- 若 X 高热但 web 缺：必须追加 1 次定向 web_search 补权威来源；补不到就写“仅社媒热度，暂无权威报道”。

5) 扩展 query（按需触发，pw，标注「7d补漏」）
- 仅触发才跑，上限 4~8 条，按缺口选择：
  - 厂商补漏：Seedance/GLM/Kimi/Qwen/混元/文心
  - 评测补漏：SWE-bench 最新评测/leaderboard
  - 商业补漏：pricing change / funding / partnership

6) 今日漏报检查（必须执行；24h 主，必要时 pw 补漏）
清单：
- 字节：Seedance/即梦/豆包
- 智谱：GLM
- 月之暗面：Kimi
- 阿里：Qwen
- 腾讯：混元
- 百度：文心
- 海外：OpenAI/Anthropic/Gemini/Meta
规则：若 X 命中但 web 缺 → 必须补 1 次定向 web_search；补不到明确标注。

7) 交付物（必须）
- 写入 data/daily.json + data/techneme.json
- python3 scripts/render.py
- bash scripts/verify.sh YYYY-MM-DD
- git commit/push

8) 页面/自检（必须输出在 HTML，风格按 2/13）
- X 高互动事件卡片（8-12条，不足如实说明）
- 覆盖度自检：
  - 每栏目条数（目标/下限/是否触发补搜）
  - 命中厂商清单（命中/未命中）
  - 触发补搜轮数、补搜 query 数
  - ≤48h 可确认条目数 vs 观察/补充数
  - dedupe_key 列表（至少 entity/product/version）

9) 推送文案（必须是最后输出，禁止再输出总结覆盖）
📰 AI日报 YYYY-MM-DD

1) 一句话结论 — 来源：<url>
2) ...
3) ...
4) ...
5) ...

🔗 在线阅读: <summary.url>
📁 归档: <summary.archiveUrl>

规则：严格 5 条；每条至少 1 个来源链接；若合并事件，优先给最权威链接。

严禁：调用 message 工具；调用 cron.run 触发其他 job；输出状态总结覆盖推送文案。
